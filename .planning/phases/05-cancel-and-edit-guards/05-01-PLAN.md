# 05-01-PLAN — Schema truth: remembering the status a cancellation replaced

**Read `05-CONTEXT.md` first.** §4.1 is the decision this plan implements; §9 lists the standing
constraints that bind every task here.

One task. It is dispatchable immediately and has **no dependency on anything**, including the two
tasks in `05-02-PLAN.md`, which it likewise does not gate — see the dispatch note in `ROADMAP.md`.

---

## Frozen contract — what `status_before_cancel` means

Written down here because three separate pieces of code key on it and they must agree.

- `status_before_cancel` holds **the status the row had immediately before it became `cancelled`**,
  and nothing else. It is never a preference, never a target, never a default.
- It is **non-NULL only while `status = 'cancelled'`.** Enforced by CHECK, not by convention.
- **NULL while cancelled is legal and means "not recorded"** — every row cancelled before this
  migration, and any row an operator cancels by hand in SQL. Consumers restore such a row to
  `'scheduled'`, which is exactly today's behaviour.
- The domain is `'scheduled' | 'in_progress' | 'completed'`. `'cancelled'` is **not** in it: a
  cancellation cannot have replaced a cancellation.
- `'completed'` is in the domain and is **currently unreachable through the application** (REQ-024
  refuses that cancellation). The schema deliberately does not encode that application policy.
- Every writer sets `status` and `status_before_cancel` **in the same UPDATE statement**. There is
  no code path anywhere that writes one without the other. The coherence CHECK makes any attempt
  fail loudly instead of leaving a lie in the table.

---

## Task 05-01-T1 — The migration, and the one type it changes

**Satisfies**: REQ-025 (its schema half).
**Agent**: `coder-sr`
**Depends on**: nothing. **Dispatchable immediately.**

### Files owned (exclusive)

```
supabase/migrations/20260922120000_appointments_status_before_cancel.sql   (new file)
src/types/database.ts                                                      (one table, three blocks)
```

No other task in this phase may write either file.

### What to do

**The migration.** Create
`supabase/migrations/20260922120000_appointments_status_before_cancel.sql`. The filename follows
constitution §12 (`YYYYMMDDHHMMSS_description.sql`) and takes the next slot after
`20260921130000_void_releases_appointments.sql`.

Copy the **header conventions** of that file exactly — it is the precedent for this project and a
reviewer will diff the two. The header is a comment block that must state, in this order:

1. **What the column is for, in prose**, citing the defect: `cancelAppointment`
   (`src/lib/actions/appointments.ts:895-920`) accepts any status and `uncancelAppointment`
   (`:922-948`) restores the literal `'scheduled'`, so *complete → cancel → reopen* destroys the
   only record that the work was done. Name GitHub issue **#15** and REQ-025.
2. **The frozen contract**, reproduced from this plan's section above. All six bullets.
3. **Why `'completed'` is in the domain CHECK although REQ-024 makes it unreachable.** Use the
   wording in `05-CONTEXT.md §4.1`: the schema is not the place to encode an application policy a
   later product decision may reverse. A reviewer reading the CHECK against REQ-024 must find this
   answer in the file, not have to ask for it.
4. **BACKFILL: none, stated explicitly (constitution §4).** No row is inserted, updated or deleted.
   Existing rows — **including rows already at `status = 'cancelled'`** — keep NULL, and NULL means
   "not recorded", which consumers restore to `'scheduled'`: byte-for-byte today's behaviour. No
   stored value changes meaning and no amount anywhere is touched, so constitution §7 is satisfied
   with **no exception required** and none is claimed.
5. **CONSTITUTION §4 classification**, in those words: this is **additive**. One nullable column and
   two CHECK constraints, no column dropped, renamed or retyped, no row rewritten. The two CHECKs
   cannot fail on existing data, and say why — every existing row has `status_before_cancel IS NULL`
   the instant the column is added, and `IS NULL` satisfies both constraints unconditionally. **No
   ARCH-1-style human-approved exception is needed and none is claimed.** The approving requirement
   is REQ-025.
6. **DOWN**, as verified-executable SQL in the header, exactly two statements in this order:

   ```
   --   ALTER TABLE "public"."appointments"
   --       DROP CONSTRAINT "appointments_status_before_cancel_only_when_cancelled";
   --
   --   ALTER TABLE "public"."appointments"
   --       DROP CONSTRAINT "appointments_status_before_cancel_check";
   --
   --   ALTER TABLE "public"."appointments" DROP COLUMN "status_before_cancel";
   ```

   (Three statements; the constraints go first so the drop cannot trip over them, even though
   `DROP COLUMN` would cascade to them anyway — being explicit is the precedent in
   `20260921130000`.) State plainly that the DOWN **discards every recorded prior status**, which is
   the correct behaviour for reverting an additive column and is not a §7 violation because it
   returns the table to the exact shape and content it had before the UP ran.

   **Ship the header with the "NOT yet executed" wording**, precisely as
   `20260921120000_client_job_pricing_partial_unique.sql` did, because per D-41 **you do not start a
   database**. The verifier executes the DOWN in its serial pass and reports the corrected header
   line back for the orchestrator to apply as a one-line edit.

**The DDL**, after the header. Three statements plus two comments:

```sql
ALTER TABLE "public"."appointments"
    ADD COLUMN "status_before_cancel" "text";

ALTER TABLE "public"."appointments"
    ADD CONSTRAINT "appointments_status_before_cancel_check"
    CHECK (("status_before_cancel" IS NULL) OR ("status_before_cancel" = ANY (ARRAY['scheduled'::"text", 'in_progress'::"text", 'completed'::"text"])));

ALTER TABLE "public"."appointments"
    ADD CONSTRAINT "appointments_status_before_cancel_only_when_cancelled"
    CHECK (("status_before_cancel" IS NULL) OR ("status" = 'cancelled'::"text"));
```

- The quoting and the `= ANY (ARRAY[...])` shape mirror `appointments_status_check`
  (`20260427000000_schema_snapshot.sql:175`) — match the existing file's style, not your own.
- **Two constraints, not one.** They fail for different reasons and a combined constraint would
  report the wrong one. Keep the names above verbatim; the verifier asserts on them by name.
- The column is **nullable with no DEFAULT**. Do not add `DEFAULT NULL` (noise) and do not make it
  `NOT NULL` (it would need a backfill, which §4 of the header just promised there is none of).
- Add a `COMMENT ON COLUMN "public"."appointments"."status_before_cancel"` carrying the frozen
  contract in one paragraph, in the style of the two `COMMENT ON` statements at the end of
  `20260921130000`. It must name its writers explicitly: **`cancelAppointment` and
  `updateAppointment` set it, `uncancelAppointment` clears it, and nothing else writes it.**

**`src/types/database.ts`** — the matching hand edit, required in this same task by constitution §9.
The `appointments` table's three blocks are at roughly `:296-345`. Add, in each, immediately after
the existing `status` line so the file reads in column order:

- `Row`: `status_before_cancel: 'scheduled' | 'in_progress' | 'completed' | null`
- `Insert`: `status_before_cancel?: 'scheduled' | 'in_progress' | 'completed' | null`
- `Update`: `status_before_cancel?: 'scheduled' | 'in_progress' | 'completed' | null`

Type it to the **real** nullability (§9). The union excludes `'cancelled'` because the CHECK does;
do not widen it to the four-value `AppointmentStatus` union for symmetry. Nothing else in this file
changes — not `Relationships`, not `Functions`, not any other table.

### Acceptance criteria

**[static] — run by `coder-sr`, no database:**

1. `git status --short` lists your two owned paths and **no other file under `src/`,
   `supabase/` or `scripts/`**. Files under `.planning/` are the orchestrator's and are expected to
   be dirty; ignore them. Nothing is staged and nothing is committed.
2. The new migration's filename sorts strictly after `20260921130000_void_releases_appointments.sql`
   and matches `YYYYMMDDHHMMSS_description.sql`.
3. The header contains all six numbered items above, including the literal phrase
   `BACKFILL: none`, an explicit §4 additive classification, and a DOWN block marked **not yet
   executed**.
4. `grep -n "status_before_cancel" src/types/database.ts` returns exactly three lines, all inside
   the `appointments` table's `Row` / `Insert` / `Update` blocks.
5. `npx tsc --noEmit` is clean. (D-22: this proves only that the file is well-formed TypeScript.
   Do not cite it as evidence of anything else.)
6. `npm run lint` shows **no new** errors. Six pre-existing errors are expected in files this
   milestone never touched; report the count, do not fix them.
7. `git diff supabase/migrations/` shows **no** modification to any previously landed migration.

**[verifier] — executed once in the serial pass (E1–E4 below). Do not run these yourself.**

### Out of scope for this task

- Any edit to `src/lib/actions/appointments.ts` or to any page. Those belong to `05-02`.
- Any change to `appointments_employee_view` or any other view. The view lists its columns
  explicitly (`05-CONTEXT.md §2.12`), so the new column is already invisible to employees. Adding it
  would be a privacy regression **and** a constitution §10 violation.
- Any new invariant in `supabase/checks/pricing_backfill_check.sql`. The coherence CHECK makes the
  only candidate invariant unrepresentable in data; constitution §14 forbids guarding it anyway.
- A `NOT NULL` column, a `DEFAULT`, a backfill, an `UPDATE`, or any enum type. `text` + CHECK is the
  established pattern for `appointments.status` and this column matches it.
- Adding `'cancelled'` to the domain CHECK.
- Starting, resetting, seeding or querying any database (D-41).
- Committing anything.

---

## Verification (for `verifier`)

This runs **after both `05-02` tasks have landed as well**. It is one serial pass; nothing else may
touch the database while it runs (D-41).

### Changed-risk surface

One new nullable column on `appointments` and two new CHECK constraints on a table every admin
screen reads, plus three lines of hand-maintained types. The risks are: a CHECK that rejects a write
the application legitimately makes (which shows up as a 500 on Cancel), a DOWN path that does not
execute, and a column that quietly reaches the employee view.

### Commands, narrowest first

```
npx tsc --noEmit
npm run lint
npm test
npm run build
supabase start          # if not already running
supabase db reset
```

`supabase db reset` must apply all nine migrations in timestamp order with no error. Per
constitution §16, **its exit code is not evidence** — every claim below is a row count or a catalog
read taken afterwards.

### E1 — the column and the constraints exist, with the right shape

```sql
SELECT column_name, data_type, is_nullable, column_default
  FROM information_schema.columns
 WHERE table_schema = 'public' AND table_name = 'appointments'
   AND column_name = 'status_before_cancel';
```

Expect exactly one row: `text`, `YES`, `NULL`.

```sql
SELECT conname, pg_get_constraintdef(oid)
  FROM pg_constraint
 WHERE conrelid = 'public.appointments'::regclass AND contype = 'c'
 ORDER BY conname;
```

Expect **three** CHECK constraints: the pre-existing `appointments_status_check`, plus
`appointments_status_before_cancel_check` and
`appointments_status_before_cancel_only_when_cancelled`. Quote all three definitions in the report.

### E2 — no backfill happened

```sql
SELECT count(*) AS must_be_0 FROM public.appointments WHERE status_before_cancel IS NOT NULL;
```

Expect **0** on a freshly reset, seeded database.

### E3 — both CHECKs enforced, in both directions, on real rows

Take any seeded appointment id. Run all six, recording which raise `23514` and which report
`UPDATE 1`:

1. `UPDATE public.appointments SET status_before_cancel = 'scheduled' WHERE id = <A>;`
   → **must fail** (row is not cancelled) — the coherence CHECK.
2. `UPDATE public.appointments SET status = 'cancelled', status_before_cancel = 'scheduled' WHERE id = <A>;`
   → **must succeed**, `UPDATE 1`.
3. `UPDATE public.appointments SET status_before_cancel = 'in_progress' WHERE id = <A>;`
   → **must succeed** (already cancelled).
4. `UPDATE public.appointments SET status_before_cancel = 'cancelled' WHERE id = <A>;`
   → **must fail** — the domain CHECK. This is the one that proves `'cancelled'` was excluded.
5. `UPDATE public.appointments SET status_before_cancel = 'bogus' WHERE id = <A>;`
   → **must fail** — the domain CHECK.
6. `UPDATE public.appointments SET status = 'scheduled', status_before_cancel = NULL WHERE id = <A>;`
   → **must succeed**, and returns the row to its seeded state.

A pass needs all six outcomes, not five. Report the actual SQLSTATE of each failure.

### E3b — the column is not visible to employees

```sql
SELECT column_name FROM information_schema.columns
 WHERE table_schema = 'public' AND table_name = 'appointments_employee_view'
 ORDER BY ordinal_position;
```

`status_before_cancel` **must not appear**. Thirteen columns, unchanged from
`20260918130000_restrict_employee_price_visibility.sql:107-120`.

### E4 — the DOWN path, executed

On the reset database, after E1–E3b, run the three DOWN statements from the migration header
verbatim. All three must succeed. Then re-assert E1's first query: it must return **zero** rows, and
the two new constraints must be gone from E1's second query while `appointments_status_check`
remains. Then `supabase db reset` again to restore the UP state before the user's browser pass.

**Then report the header correction.** The verifier cannot edit files. On success, quote the exact
header line to change and the exact replacement — the same "executed <date> against a freshly reset
local database" wording `20260921130000:56-57` carries — for the orchestrator to dispatch as a
one-line edit.

> **CLOSED 2026-09-22.** This ran as specified: the verifier executed the DOWN path in round 1 and
> re-applied UP, and the orchestrator made the one-line header edit —
> `supabase/migrations/20260922120000_appointments_status_before_cancel.sql:54` now reads
> `-- DOWN (executed 2026-09-22 against a freshly reset local database). Reverses the schema
> exactly.` No requirement or plan text changes; recorded so the "NOT yet executed" instruction at
> line 94 is not read later as unfinished work.

### PASS requires

- E1: one row, `text` / `YES` / `NULL`; three CHECK constraints present with the specified names.
- E2: `0`.
- E3: all six outcomes exactly as specified, with SQLSTATEs quoted.
- E3b: `status_before_cancel` absent from the employee view.
- E4: all three DOWN statements execute, the column and both constraints disappear, and the UP state
  is restored afterwards.
- `npx tsc --noEmit` clean, `npm test` 17/17, `npm run build` succeeds, `npm run lint` at its
  pre-existing error count and no more.
- **Every criterion above is satisfied for REQ-025's schema half only.** REQ-025's behaviour, and
  all of REQ-024, REQ-026 and REQ-027, are proven by the user's browser script in
  `05-02-PLAN.md`. This task cannot PASS the phase on its own.

**Verdict contract — PASS or BLOCKED, nothing in between** (D-34). Until the user reports the
browser pass back, the phase verdict is BLOCKED and the next action is to relay the script.
